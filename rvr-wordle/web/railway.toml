[build]
  builder = "nixpacks"

[deploy]
  startCommand = "npm run build && npx serve dist -p 8080 -s"
  port = 8080
  healthcheckPath = "/"
  healthcheckTimeout = 100

[env]
  NODE_ENV = "production"
  VITE_API_URL = "https://worker-production-cbde.up.railway.app"
